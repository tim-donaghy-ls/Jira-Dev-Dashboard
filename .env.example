# ============================================
# JIRA DASHBOARD - UNIFIED CONFIGURATION
# ============================================
# Copy this file to .env and fill in your actual values

# ============================================
# BACKEND (Go Server) Configuration
# ============================================

# Primary JIRA Instance
PRIMARY_JIRA_NAME=Your Company Name
PRIMARY_JIRA_BASE_URL=https://your-domain.atlassian.net
PRIMARY_JIRA_EMAIL=your-email@company.com
PRIMARY_JIRA_API_TOKEN=your-jira-api-token-here

# Secondary JIRA Instance (Optional - leave blank if not needed)
SECONDARY_JIRA_NAME=
SECONDARY_JIRA_BASE_URL=
SECONDARY_JIRA_EMAIL=
SECONDARY_JIRA_API_TOKEN=

# Server Configuration
SERVER_PORT=8080

# ============================================
# FRONTEND (Next.js) Configuration
# ============================================

# API Endpoint - Points to the Go backend
NEXT_PUBLIC_API_URL=http://localhost:8080

# Anthropic API Key (for AI Release Notes feature)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ============================================
# AUTHENTICATION (Google OAuth) Configuration
# ============================================

# Google OAuth Credentials (create at: https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here-generate-with-openssl-rand-base64-32

# User Access Control - Comma-separated list of allowed email addresses
# Example: ALLOWED_EMAILS=user1@company.com,user2@company.com,admin@company.com
# Leave empty to allow all Google users (not recommended for production)
ALLOWED_EMAILS=your-email@company.com

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Generate JIRA API Token: https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Get Anthropic API Key: https://console.anthropic.com/
# 3. Set up Google OAuth:
#    a. Go to https://console.cloud.google.com/apis/credentials
#    b. Create a new project or select an existing one
#    c. Enable Google+ API
#    d. Create OAuth 2.0 Client ID (Web application)
#    e. Add authorized redirect URIs:
#       - http://localhost:3000/api/auth/callback/google (development)
#       - https://your-domain.com/api/auth/callback/google (production)
# 4. Generate NextAuth Secret: openssl rand -base64 32
# 5. Copy this file: cp .env.example .env
# 6. Fill in your actual values in the .env file
# 7. Never commit the .env file to version control
